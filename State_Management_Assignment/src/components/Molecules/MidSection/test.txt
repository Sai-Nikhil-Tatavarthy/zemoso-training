import React from "react";
import CheckboxTable from "../ContractsTable";
import "./style.css";

interface Props {
  onSelectedRowsChange: (selected: number[]) => void;
}

const MidSection = (props: Props) => {
  //const [selectedRows, setSelectedRows] = React.useState<number[]>([]);

  // const handleSelectedRowsChange = (selected: number[]) => {
  //   setSelectedRows(selected);
  // };

  return (
    <div>
      <div className="mid_sec_heading">
        <h2>Your Contracts </h2>
      </div>
      <div>
        <CheckboxTable onSelectedRowsChange={props.onSelectedRowsChange} />
      </div>
    </div>
  );
};

export default MidSection;
